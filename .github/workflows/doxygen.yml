name: Doxygen

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: DenverCoder1/doxygen-github-pages-action@v1.2.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages
          folder: /home/runner/work/TechnicCraft/TechnicCraft/Documentation/html
          config_file: /home/runner/work/TechnicCraft/TechnicCraft/Documentation/Doxyfile